import { Callout, ParamTable } from "../../components/Mdx/Mdx";
import { StacksSignMessageExample } from "../examples/StacksSignMessageExample";

# stx_signMessage

Use the <code>stx_signMessage</code> method to request the user to sign a message with their Stacks address. This is useful for authentication and proof-of-ownership.

## Parameters

<ParamTable
  params={[
    {
      name: "message",
      type: "string",
      required: true,
      description: "The message to sign.",
    },
  ]}
/>

## Try it

<StacksSignMessageExample />

## Response

<ParamTable
  params={[
    {
      name: "signature",
      type: "string",
      required: true,
      description: "The message signature.",
    },
    {
      name: "publicKey",
      type: "string",
      required: true,
      description: "The public key used for signing.",
    },
  ]}
/>
